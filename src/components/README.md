Design System — Component Structure

This folder contains component groups following the Design Systems Architect rules:

- Atomic levels: `atoms/`, `molecules/`, `organisms/`.
- Strict BEM naming required for all CSS class names (block__element--modifier).
- Use semantic HTML; avoid non-semantic interactive wrappers.

See: .github/agents/design-systems-architect-agent.agent.md for the full rules.
