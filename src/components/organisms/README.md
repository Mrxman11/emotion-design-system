Organisms

Purpose: Complex UI sections composed of molecules and atoms (e.g., `Header`, `Footer`, `Nav`).
Placement rationale: Organisms form distinct sections of the UI and may include layout concerns.
Rules:
- Use semantic landmarks (`<header>`, `<nav>`, `<main>`, `<footer>`).
- Do not use `<div>` as interactive elements; prefer semantic tags.
