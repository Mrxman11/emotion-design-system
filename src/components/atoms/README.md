Atoms

Purpose: Small, single-purpose components (e.g., `Button`, `Icon`, `Input`).
Placement rationale: Atoms are the smallest building blocks with no child components.
Rules:
- Follow BEM naming: `button__icon--small`.
- Use semantic elements (e.g., `<button>`, `<input>`).
