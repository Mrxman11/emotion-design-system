Molecules

Purpose: Composed components built from atoms (e.g., `FormField`, `SearchBar`).
Placement rationale: Molecules combine atoms to provide a small UI pattern.
Rules:
- Keep markup semantic (use `<label>`, `<fieldset>`, etc.).
- CSS must follow BEM and live with the component.
